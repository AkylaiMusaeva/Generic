package practice3;

public class Main {
    public static void main(String[] args) {
        /**class Product<T extends Enum<T>> {
         category  (enum: phone, laptop)
         name, characters  Map<KeyCharacter, String>) ,price.
         KeyCharacter(enum: 4 характеристик)
         public String saveProduct(.............);
         public static <T extends Enum<T>> List<Product<T>>
         searchByProductName(String name)
         public static <T extends Enum<T>> List<Product<T>>
         filterByCharacter(List<Product<T>> products, KeyCharacter key, String value)
         public static <T extends Enum<T>> void sortByCharacter(List<Product<T>>
         products, KeyCharacter key)*/




    }
}
